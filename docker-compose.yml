version: '3.8'

services:
  frontend:
    build: .
    # No external port: Caddy reverse-proxies to it as rsgo-frontend:80 on the shared network.
    restart: unless-stopped
    container_name: rsgo-frontend
    networks:
      - rsgo-shared-network

networks:
  rsgo-shared-network:
    external: true